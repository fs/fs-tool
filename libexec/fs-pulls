#!/usr/bin/env bash
# Usage: fs pulls
# Summary: Open Flatstack pull requests on github
# Help: Open Flatstack pull requests on github. Available subcommands:
#
#    fs pulls                       # opens public Flatstack pull requests on github
#    fs pulls private               # opens private Flatstack pull requests on github

set -e

# Provide fs completions
if [ "$1" = "--complete" ]; then
  echo public
  echo private
else
  case $1 in
    "" | "public")
    fs-open "https://github.com/organizations/fs/dashboard/pulls/public"
    ;;
    "private")
    fs-open "https://github.com/organizations/fs/dashboard/pulls/private"
    ;;
  esac
fi
