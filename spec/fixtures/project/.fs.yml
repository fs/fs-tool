# project configuration file
servers:
  default: &default
    address: foo@bar.com
    environment: staging
    root: /data/application
  staging:
    <<: *default
  staging_2:
    <<: *default
    address: foo@bar2.com
